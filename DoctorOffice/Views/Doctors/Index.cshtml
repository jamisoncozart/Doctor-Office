@{
  Layout = "_Layout";
}

@using DoctorOffice.Models;
@model List<DoctorOffice.Models.Doctor>;

<h1>DOCTORS</h1>

@if (@Model.Count == 0)
{
  <h3>No doctors have been added yet!</h3>
} 

@foreach (Doctor doctor in Model)
{
  <li>@Html.ActionLink($"{doctor.Name}", "Details", new { id = doctor.DoctorId })</li>
}

@Html.ActionLink("ADD NEW DOCTOR", "Create", "Doctors", null, new { @class="btn-block" })